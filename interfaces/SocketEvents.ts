export enum SocketEvents {
  login = "login",
  pause = "pause",
  resume = "resume",
  queue = "queue",
  subscribeBotConnection = "subscribeBotConnection",
  unsubscribeBotConnection = "unsubscribeBotConnection",
  

  login_client = "login_client",
  queue_client = "queue_client",
  bot_event = "bot_event",
}
